gradle build // build regular

gradle wrapper --gradle-version 2.13 //install portable gradle
./gradlew build //build with portable

activate workBench :
mysql-workbench

to run server in background :
nohup gradle run

to find the pid of the process in background:
ps -ef | grep "gradle run"
